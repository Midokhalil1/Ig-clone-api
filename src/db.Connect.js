import { initializedApp, cert, getApps } from "firebase-admin";
import { getFirestore } from "firebase-admin/firestore";
import secrets from "../functions/secrets";

export default function dbConnect() {
    
}